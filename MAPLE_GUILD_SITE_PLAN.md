# 메이플 길드 주간 점수 관리 사이트 구축안 (초저비용, 다중 이미지 업로드 대응)

## 1) 목표 정리
- 길드원 계정으로 로그인 가능한 웹사이트
- 매주 목요일마다 **여러 장(평균 10장)**의 길드 미션/수로 점수 스크린샷 업로드
- 이미지에서 유저명 + 점수 자동 추출(OCR)
- 규칙에 따라 직위 승급/강등 추천 자동 계산
- 주차별 데이터 누적 저장(통계 기능은 이후 확장)

---

## 2) 비용 0원~연 10달러 이하로 가능한 현실적 구성

> 현재 구현본(MVP)은 **Supabase Postgres + Flask** 기준으로 먼저 시작하고, 추후 Cloudflare 스택으로 이관 가능하게 설계합니다.

### 추천 1안 (가장 쉬움, 거의 0원)
- **프론트/백엔드**: Cloudflare Workers + Cloudflare Pages (무료)
- **DB**: Cloudflare D1 (무료 티어)
- **파일 저장소(이미지)**: Cloudflare R2 (무료 티어 내에서 사용 가능)
- **인증**: Cloudflare Access 또는 간단한 자체 이메일/비밀번호 인증 (무료)
- **OCR**:
  - 1차: 클라이언트(브라우저)에서 Tesseract.js 수행 (서버비 0원)
  - 2차: 서버에서 규칙 기반 파서 + 유사도 매칭 + 수동 검수 UI

> 주 10장 업로드(월 40~50장) 규모는 무료 티어로 충분히 대응 가능.

### 추천 2안 (백업 대안)
- **앱**: Supabase 무료 티어 + Vercel 무료 티어
- **DB/인증/스토리지**: Supabase 통합 사용
- **OCR**: 클라이언트 Tesseract.js 또는 Supabase Edge Function + OCR 라이브러리

> 무료 티어 정책 변동 가능성 때문에, 장기적으로는 Cloudflare 조합이 안정적.

---

## 3) 핵심 쟁점: 이미지 분석(OCR) 정확도 설계

메이플 점수판 스샷은 폰트/해상도/압축 상태에 따라 OCR 오차가 생길 수 있으므로,
**"완전 자동"보다 "자동 + 검수" 구조가 필수**입니다.

### 다중 이미지(주 10장) 처리 파이프라인 (권장)
1. **주차 생성 + 일괄 업로드**
   - 한 번에 10장까지 드래그앤드롭 업로드
   - 업로드 즉시 이미지 해시(phash) 생성으로 중복 파일 감지
2. **전처리 (이미지별)**
   - 그레이스케일
   - 대비 향상(CLAHE)
   - 이진화(Adaptive Threshold)
   - 2x 확대 + 샤픈
3. **ROI(관심영역) 고정 크롭**
   - 점수/닉네임이 보이는 구역만 잘라 OCR 수행
   - UI 위치가 일정하면 정확도 크게 상승
4. **OCR 실행 (2패스)**
   - 1패스: 숫자 우선(점수 컬럼)
   - 2패스: 한글/영문 혼합(닉네임 컬럼)
   - 각 토큰 confidence 저장
5. **행 단위 파싱 + 유효성 검증**
   - `닉네임 + 점수` 정규식 추출
   - 점수 범위/형식 검증(예: 0~99999)
6. **길드원 매칭 (오타 보정 포함)**
   - 길드원 마스터와 유사도 매칭(Levenshtein + Jaro-Winkler)
   - `l/I`, `O/0`, `S/5` 등 자주 나는 OCR 오타 치환
7. **주차 단위 통합(Consensus Merge)**
   - 같은 유저가 여러 이미지에 등장하면
     - 점수: 다수결/중앙값 우선
     - 이름: 사전 일치 우선
   - 충돌 건은 "검수 필요"로 큐에 적재
8. **검수 화면 제공**
   - confidence 낮은 항목만 빨간색 강조
   - 원본 이미지의 해당 좌표를 바로 하이라이트
   - 관리자 1회 확인 후 저장 확정
9. **주차 데이터 저장**
   - 이미지 원본 + OCR 원문 + 확정 결과를 분리 저장

### 정확도 올리는 실무 팁 (중요)
- 업로드 가이드 고정
  - 1920x1080 이상
  - UI 스케일 고정(예: 100%)
  - JPG보다 PNG 권장
- 촬영 표준화
  - 가능하면 동일 해상도/동일 UI 테마로 캡처
  - 점수판 전체가 잘리거나 흐리지 않게 안내
- 닉네임 사전 강화
  - 실제 길드원 닉네임 + 자주 틀리는 별칭/오타 사전 함께 관리
- 자동 재시도
  - confidence 평균이 낮은 이미지는 전처리 파라미터를 바꿔 1회 재OCR
- 최종 규칙
  - 자동 확정 임계치(예: confidence 92% 이상)
  - 그 이하 항목만 사람 검수

---

## 4) 최소 기능(MVP) 설계

### 사용자 역할
- **관리자**: 길드원 관리, 이미지 업로드/검수, 승급/강등 확정
- **일반 길드원(선택)**: 본인 기록 조회만

### 화면 구성
1. 로그인 페이지
2. 길드원 목록/직위 관리 페이지
3. **주간 점수 다중 업로드 페이지(최소 10장 대응)**
4. OCR 결과 검수 페이지(신뢰도 낮은 항목만 우선 노출)
5. 주간 결과 페이지(승급/강등 추천 포함)

### 데이터 모델(초기)
- `users` (id, email, role, created_at)
- `members` (id, nickname, job, rank, is_active, nickname_aliases)
- `weekly_batches` (id, week_key, uploaded_count, status, created_by)
- `images` (id, batch_id, file_url, image_hash, uploaded_at)
- `ocr_lines` (id, image_id, line_no, raw_name, raw_score, confidence)
- `weekly_records` (id, week_key, member_id, score, source_image_id, confirmed)
- `rank_rules` (id, rank_name, min_score, max_miss_count, promote_to, demote_to)

---

## 5) 승급/강등 자동 판정 규칙 예시

규칙 엔진은 간단한 테이블 기반으로 시작하세요.

예시:
- 최근 4주 평균 점수 ≥ 1200 → 1단계 승급 후보
- 최근 2주 연속 기준 미달(<700) → 1단계 강등 후보
- 미참여 주차 2회 이상 → 경고

출력은 **"자동 확정"이 아니라 "추천"**으로 두고, 관리자가 최종 승인하도록 설계하면 분쟁이 줄어듭니다.

---

## 6) 개발 단계(현실적인 순서)

### 1단계 (1~2주)
- 로그인
- 길드원 CRUD
- 주차 생성
- **다중 이미지 업로드(최소 10장)**
- OCR + 검수 저장

### 2단계 (1주)
- 주차 단위 통합(중복/충돌 해결)
- 승급/강등 추천 규칙 적용
- 주간 결과 리포트 화면

### 3단계 (나중에)
- 통계 대시보드
  - 개인 추세
  - 직위별 평균
  - 참여율

---

## 7) 연 10달러 이하 유지 전략

- OCR은 외부 유료 API(Google Vision/Naver CLOVA) 대신 **Tesseract.js 로컬/브라우저 처리**
- 이미지 보관 기간 정책 설정(예: 원본 12주 후 자동 삭제, 텍스트 결과만 영구 보관)
- 관리자 전용 사이트로 제한해 트래픽 최소화
- 백업은 월 1회 CSV export로 수동 보관
- 썸네일만 상시 보관하고 원본은 보관 기간 이후 자동 삭제

이렇게 하면 보통 **0원 운영**이 가능하고,
도메인을 붙이고 싶다면 `.xyz`/`.site` 저가 도메인만 연 8~10달러 수준으로 추가됩니다.

---

## 8) 바로 시작 가능한 기술 스택 (권장 고정안)

- Frontend: Next.js
- Backend API: Cloudflare Workers
- DB: Cloudflare D1 (SQLite)
- Storage: Cloudflare R2
- OCR: Tesseract.js + OpenCV.js(전처리)
- Queue(선택): Cloudflare Queues (다중 업로드 비동기 처리용)
- Auth: Cloudflare Access 또는 자체 세션

> 이후 통계를 붙일 때도 동일 스택으로 확장 가능.

---

## 9) 리스크와 대응

- OCR 오인식 리스크
  - 대응: 검수 UI + 닉네임 사전 + 촬영 가이드 + 다중 이미지 합의(Consensus)
- 이미지 중복 업로드 리스크
  - 대응: 해시 기반 중복 감지 + 배치 상태 관리
- 규칙 분쟁 리스크
  - 대응: 자동 "추천" + 관리자 확정 로그 저장
- 무료 티어 정책 변경 리스크
  - 대응: 데이터 export/import 스크립트 준비

---

## 10) 결론

요구사항 기준으로는 **"Cloudflare 무료 스택 + Tesseract.js + 관리자 검수 + 다중 이미지 합의 로직"** 조합이
가장 저렴하고(사실상 0원), 구현 난이도도 낮으며, 정확도/운영 안정성에 유리합니다.

원하면 다음 단계로 실제 구현용 **DB 스키마 SQL, OCR 파이프라인 의사코드, API 명세, 검수 화면 와이어프레임**까지 바로 작성할 수 있습니다.
